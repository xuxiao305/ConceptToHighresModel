# Page3 Source Viewport 多模型加载功能完整需求文档

## 一、功能整体说明
本次针对 Page3 页面 Source 板块下的 Viewport，开展多模型加载及配套交互、渲染功能全量扩展，完善模型加载卸载、列表管理、选中标识、姿态调节、对齐及渲染展示能力，同时对齐 Page1、Page2 视图的材质渲染标准，全程保留原有功能逻辑，仅做新增扩展，不改动原有可用功能。

## 二、模型加载与卸载功能
操作载体：页面下方的 Source Mesh Gallery 区域，在每个模型节点旁新增两个功能按钮；
加载按钮：点击对应按钮，可将该节点模型直接载入至 Source Mesh Viewport 中；
卸载按钮：采用减号作为按钮标识，点击后可将对应模型从 Source Mesh Viewport 中移除。

## 三、已加载模型列表功能
展示位置：Viewport 左侧区域设置专属模型列表；
展示内容：实时同步显示当前已成功加载进 Viewport 的所有模型；
交互逻辑：支持用户点击列表内任意模型条目，可依次选中不同模型，明确当前操作对象。

## 四、模型选中高亮功能
核心目的：让用户直观识别当前选中的模型，为后续对齐操作提供清晰指引；
实现方案：支持材质高亮、轮廓勾边高亮两种方式，优先选用开发实现更简单的方案；
效果要求：选中状态区分度明显，能快速和未选中模型形成视觉差异。

## 五、Viewport 模型操控功能
基础姿态调节：Source Viewport 新增交互能力，支持对视图内模型进行移动、旋转操作，灵活调整模型位置与角度；
高级参数调节：支持手动调节模型的坐标中心，满足模型精细化摆放、对齐的操作需求。

## 六、Viewport 显示模式扩展功能
原有模式保留：完全保留 Viewport 当前所有显示模式，不修改原有渲染逻辑；
新增渲染模式：新增材质贴图渲染模式，该模式的渲染效果、材质展示标准，与 Page1、Page2 视图完全一致；
效果优化：解决当前 Viewport Solid 模式仅显示纯色、无贴图材质渲染的问题，新增模式可完整展示模型自带贴图、材质纹理，还原模型真实材质效果。