Page3 对齐 UI 重构 — 操作流程 + UI 设计摘要
当前布局
问题：对齐相关内容散布在左右两侧。左侧有一键按钮+SAM3状态，右侧有结果+重跑+手动步骤。用户视线要来回跳。

新布局
核心改动：左侧只留「模型输入 + Gallery」，所有对齐流程搬到右侧面板，按从上到下的操作顺序排列。

右侧面板分区设计
❶ 策略选择（常驻顶部）
三张卡片式选项，选中态高亮。每张卡片包含：

策略名称 + 一行适用场景
前置条件状态图标（绿勾 / 黄警 / 灰缺）
❷ 前置数据（展示当前策略的数据就绪状态）
内容根据所选策略动态变化，每条数据显示：状态图标 + 数据名 + 值/来源

三模式共需：

SAM3 分割包：已加载 / 未加载
目标区域：label + 顶点数
Source mesh / Target mesh
Pose Proxy 额外需要：

Page2 Pipeline Joints：来自哪条pipeline
Page1 Front Segment：是否可用
DWPose 服务：是否在线
orthoCamera：是否已渲染
Limb Structure 额外需要：

Body/Torso 区域：是否存在（用于root端判断）
Surface RANSAC 额外需要：

邻接表：是否已构建
缺失的前置条件显示为黄色警告 + 具体说明（如"请先在Page2生成Joint Info"）。

❸ 执行（一键按钮 + 进度）
一个主按钮，文案跟随策略："一键 Pose Proxy 对齐" / "一键四肢结构对齐" / "一键 RANSAC 对齐"
disabled 时 tooltip 说明缺什么
执行时显示当前步骤进度（从 trace 中提取关键阶段）
❹ 结果摘要（对齐完成后显示）
质量标签用颜色区分
"切换策略重跑"展开三个快捷按钮
❺ 调试/高级（默认折叠）
诊断日志（trace）
RANSAC 参数滑条（仅 surface 模式下显示）
ICP 参数滑条
手动 Step 1/2/3 分步按钮
Landmark Pairs 列表
Landmark 大小调节